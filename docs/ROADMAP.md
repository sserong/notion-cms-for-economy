# 오늘의 경제뉴스 겟 블로그 MVP 개발 로드맵

Notion을 CMS로 활용해 작성한 글이 자동으로 블로그에 반영되는 1인 경제뉴스 블로그 서비스

## 개요

**오늘의 경제뉴스 겟 블로그**는 경제뉴스를 정기적으로 읽고 싶은 독자와 Notion으로 콘텐츠를 관리하는 1인 블로거를 위한 서비스로 다음 기능을 제공합니다:

- **Notion CMS 연동**: Notion 데이터베이스에 작성한 글이 별도 발행 작업 없이 블로그에 자동 반영
- **카테고리/검색 기반 탐색**: 주식/부동산/거시경제/기업·산업 카테고리 필터와 제목 실시간 검색으로 관심 콘텐츠를 빠르게 찾기
- **반응형 독서 경험**: 모바일/태블릿/데스크톱에 최적화된 레이아웃과 가독성 높은 본문 렌더링

## 개발 워크플로우

1. **작업 계획**
   - 기존 코드베이스를 학습하고 현재 상태를 파악
   - 새로운 작업을 포함하도록 `ROADMAP.md` 업데이트
   - 우선순위 작업은 마지막 완료된 작업 다음에 삽입

2. **작업 생성**
   - 기존 코드베이스를 학습하고 현재 상태를 파악
   - `/tasks` 디렉토리에 새 작업 파일 생성
   - 명명 형식: `XXX-description.md` (예: `001-setup.md`)
   - 고수준 명세서, 관련 파일, 수락 기준, 구현 단계 포함
   - **API/비즈니스 로직 작업 시 "## 테스트 체크리스트" 섹션 필수 포함 (Playwright MCP 테스트 시나리오 작성)**
   - 예시를 위해 `/tasks` 디렉토리의 마지막 완료된 작업 참조 (예: 현재 작업이 `005`라면 `004`, `003` 참조)
   - 새 작업의 경우, 문서에는 빈 박스와 변경 사항 요약이 없어야 함. 초기 상태의 샘플로 `000-sample.md` 참조

3. **작업 구현**
   - 작업 파일의 명세서를 따름
   - 기능과 기능성 구현
   - **API 연동 및 비즈니스 로직 구현 시 Playwright MCP로 테스트 수행 필수**
   - 각 단계 후 작업 파일 내 단계 진행 상황 업데이트
   - 구현 완료 후 Playwright MCP를 사용한 E2E 테스트 실행
   - 테스트 통과 확인 후 다음 단계로 진행
   - 각 단계 완료 후 중단하고 추가 지시를 기다림

4. **로드맵 업데이트**
   - 로드맵에서 완료된 작업을 ✅로 표시

## 상태 표시 범례

- **상태 아이콘**: ✅ 완료 / 🚧 진행중 / ⬜ 미시작
- **기능 ID**: PRD에 명시된 핵심 기능 식별자 (예: F001, F010)
- **예상 소요시간**: 1인 개발 기준 실작업 시간

---

## 개발 단계

### Phase 0: 프로젝트 기반 정리 ✅

스타터킷에서 불필요한 요소를 제거하고 블로그 도메인에 맞춘 기본 골격을 마련하는 단계입니다.

- **T001: 스타터킷 클린업 및 도메인 기본 구조 구축** ✅ - 완료
  - **상태**: ✅ 완료 | **기능 ID**: 인프라 | **예상 소요시간**: 4h
  - See: `/tasks/001-starter-cleaner.md`
  - ✅ 스타터킷 불필요 파일 제거 (로그인/회원가입 페이지, 예제 섹션)
  - ✅ `src/types/notion.ts` — `Post`, `NotionBlock`, `PostCategory` 타입 정의
  - ✅ `src/lib/notion.ts` — Notion API 클라이언트 기본 구조 (`getPosts`, `getPostsByCategory`, `getPostById`, `getPostBlocks`)
  - ✅ `src/app/posts/[id]/page.tsx` — 글 상세 페이지 라우트 뼈대
  - ✅ `src/app/category/[name]/page.tsx` — 카테고리 페이지 라우트 뼈대
  - ✅ `.env.local.example` — 환경 변수 예제 파일
  - ✅ 헤더/푸터/네비게이션을 블로그 구조로 교체
  - ✅ `@notionhq/client` 설치 완료

---

### Phase 1: 애플리케이션 골격 구축

UI 구현에 앞서 라우트, 레이아웃, 환경 변수 검증 등 전체 애플리케이션의 뼈대를 확정하는 단계입니다.

- **T002: 환경 변수 검증 및 Notion 클라이언트 안정화** ✅ - 완료
  - **상태**: ✅ 완료 | **기능 ID**: F010 | **예상 소요시간**: 3h
  - ✅ `src/lib/env.ts` — `NOTION_API_KEY`, `NOTION_DATABASE_ID` 필수값으로 변경 (Zod, 한국어 에러 메시지)
  - ✅ `src/lib/notion.ts` — `process.env` 직접 접근 제거, `env` 객체 참조로 전환, `getDatabaseId()` 헬퍼 삭제
  - ✅ `src/lib/notion.ts` — 4개 API 함수 (getPosts, getPostsByCategory, getPostById, getPostBlocks) try/catch + 로깅 표준화
  - ✅ `README.md` — 환경 변수 누락 시 에러 발생 안내 추가

- **T003: 라우트 구조 및 공통 레이아웃 확정** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F005 | **예상 소요시간**: 4h
  - `src/app/layout.tsx`의 메타데이터(title, description, OG) 블로그용으로 갱신
  - 홈(`/`) / 글 상세(`/posts/[id]`) / 카테고리(`/category/[name]`) 빈 페이지 구조 정리
  - `not-found.tsx`, `loading.tsx`, `error.tsx` 추가로 로딩/오류 UX 일관성 확보
  - 헤더 네비게이션에 카테고리 진입 링크(전체/주식/부동산/거시경제/기업·산업) 더미 연결

- **T004: 도메인 타입 및 유틸 보강** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F010, F011 | **예상 소요시간**: 3h
  - `Post` 타입에 요약(summary) 필드 추가 검토 및 추출 헬퍼 작성
  - 날짜 포맷 유틸(`formatDate`) 작성 (한국 로케일 기준 `YYYY.MM.DD`)
  - 카테고리 상수/색상 매핑 테이블을 `src/lib/categories.ts`로 추출
  - `src/lib/utils.ts`에 텍스트 트렁케이션 유틸 추가

---

### Phase 2: UI/UX 완성 (더미 데이터 활용)

실제 Notion 연동 이전에 더미 데이터로 모든 페이지의 UI/UX를 먼저 완성합니다. 백엔드 변경에 영향 받지 않도록 컴포넌트 레이어를 안정화하는 것이 목표입니다.

- **T005: 더미 데이터 픽스처 작성** ⬜ - 우선순위
  - **상태**: ⬜ 미시작 | **기능 ID**: F001, F002, F003 | **예상 소요시간**: 2h
  - `src/lib/fixtures/posts.ts`에 카테고리별 샘플 `Post[]` 작성
  - `src/lib/fixtures/blocks.ts`에 `paragraph/heading/list/image/code/quote/divider`를 모두 포함한 샘플 `NotionBlock[]` 작성
  - 추후 실제 API로 교체 가능한 형태(동일 시그니처)로 더미 fetch 함수 제공

- **T006: 공통 UI 컴포넌트 라이브러리 구축** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F001, F003, F005 | **예상 소요시간**: 6h
  - shadcn/ui 기반 `Badge`, `Card`, `Input`, `Button`, `Skeleton` 컴포넌트 추가/확인
  - `PostCard` 컴포넌트 (제목, 카테고리 배지, 발행일, 요약) 구현
  - `CategoryBadge` 컴포넌트 (카테고리별 색상 매핑 적용) 구현
  - `CategoryTabs` 컴포넌트 (전체 + 4개 카테고리 탭) 구현
  - `SearchBar` 컴포넌트 (제목 실시간 필터링용 controlled input) 구현

- **T007: 홈 페이지 UI 완성** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F001, F003, F004, F005 | **예상 소요시간**: 5h
  - 더미 데이터 기반으로 글 카드 그리드 렌더링 (모바일 1열 / 태블릿 2열 / 데스크톱 3열)
  - 카테고리 필터 버튼 동작 (URL 쿼리 또는 `useState`)
  - 검색바 기반 클라이언트 사이드 제목 필터링 동작
  - 빈 결과 상태(EmptyState) UI 구현

- **T008: 카테고리 페이지 UI 완성** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F003, F005 | **예상 소요시간**: 3h
  - `[name]` 동적 세그먼트에 따른 헤더(현재 카테고리 표시) 렌더링
  - 카테고리 탭으로 다른 카테고리 전환 가능
  - 잘못된 카테고리명 접근 시 404 처리
  - 홈과 동일한 카드 그리드 레이아웃 재사용

- **T009: 글 상세 페이지 UI 완성** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F002, F005, F011 | **예상 소요시간**: 5h
  - `NotionBlockRenderer` 컴포넌트로 블록 타입별 HTML 변환 (paragraph/heading_1~3/list/image/code/quote/divider)
  - 본문 최대 너비 제한(`max-w-2xl`) 및 prose 타이포그래피 적용
  - 메타 헤더(제목, 카테고리 배지, 발행일) 구현
  - 홈으로 돌아가기 버튼 및 키보드 접근성 적용
  - `next/image`로 이미지 블록 렌더링 시 외부 도메인 허용 설정

- **T010: 반응형 및 접근성 점검** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F005 | **예상 소요시간**: 3h
  - 모바일/태블릿/데스크톱 브레이크포인트별 시각 점검
  - 색상 대비, 포커스 링, 키보드 탐색 가능 여부 점검
  - Lighthouse 접근성 점수 90+ 확보

---

### Phase 3: 핵심 기능 구현 (Notion 연동)

더미 데이터를 실제 Notion API 호출로 교체하고 캐싱/에러 처리 등 운영 품질을 확보하는 단계입니다.

- **T011: Notion 데이터베이스 스키마 검증 및 매핑 확정** ⬜ - 우선순위
  - **상태**: ⬜ 미시작 | **기능 ID**: F010 | **예상 소요시간**: 3h
  - Notion DB의 속성(Title/Category/Tags/Published/Status) 실제 스키마 확인
  - `pageToPost` 매핑이 실제 응답과 일치하는지 통합 검증
  - 누락/타입 불일치 시 안전한 fallback 값 정의
  - **테스트 체크리스트**: Playwright MCP로 실제 환경에서 홈 → 카드 렌더 → 상세로 이동 시 데이터가 정상 표시되는지 검증

- **T012: 홈 페이지 Notion 연동 (F001 구현)** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F001, F010 | **예상 소요시간**: 3h
  - `getPosts()`를 서버 컴포넌트에서 호출하여 더미 데이터 교체
  - `revalidate` 옵션 또는 `unstable_cache` 적용으로 적절한 캐싱 전략 수립 (예: 60초)
  - 빈 목록/에러 상태 UI 연결
  - **테스트 체크리스트**: Playwright MCP로 발행됨 상태의 글만 노출되는지, 최신순 정렬이 맞는지 검증

- **T013: 카테고리 페이지 Notion 연동 (F003 구현)** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F003, F010 | **예상 소요시간**: 3h
  - `getPostsByCategory()` 서버 호출로 데이터 연결
  - 잘못된 카테고리명 접근 시 `notFound()` 호출
  - 카테고리 탭 클릭 시 라우팅 동작 확인
  - **테스트 체크리스트**: Playwright MCP로 4개 카테고리 각각의 결과가 올바르게 필터링되는지 E2E 검증

- **T014: 글 상세 페이지 Notion 연동 (F002, F011 구현)** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F002, F010, F011 | **예상 소요시간**: 4h
  - `getPostById()` + `getPostBlocks()` 호출로 메타+본문 렌더링
  - 존재하지 않는 ID 접근 시 404 처리
  - 이미지 블록 외부 URL을 `next.config.ts`의 `images.remotePatterns`에 화이트리스트 등록
  - **테스트 체크리스트**: Playwright MCP로 paragraph/heading/list/image/code 블록이 모두 정상 렌더링되는지 시나리오 검증

- **T015: 검색 기능 안정화 (F004 구현)** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F004 | **예상 소요시간**: 2h
  - 클라이언트 컴포넌트 분리(`'use client'`)로 SSR과 충돌 방지
  - 한글 자모 분리/공백 처리 등 검색 UX 다듬기
  - 디바운스 적용 (150ms)
  - **테스트 체크리스트**: Playwright MCP로 입력 → 결과 갱신 시나리오 검증

- **T016: 핵심 기능 통합 E2E 테스트** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: F001~F005, F010, F011 | **예상 소요시간**: 4h
  - Playwright MCP로 전체 사용자 플로우(홈 → 카테고리 → 검색 → 상세 → 홈 복귀) 검증
  - Notion API 장애 시 에러 페이지 노출 확인
  - 모바일 viewport 시나리오 추가
  - 콘솔 에러/네트워크 실패 0건 확인

---

### Phase 4: 운영 품질 및 배포

성능 최적화와 SEO/배포 자동화를 통해 실서비스 품질로 끌어올리는 단계입니다.

- **T017: 성능 최적화 및 캐싱 전략** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: 인프라 | **예상 소요시간**: 4h
  - ISR 적용(`revalidate` 600초 등) 및 `generateStaticParams` 적용 검토
  - `next/image` 이미지 사이즈/포맷 최적화
  - Lighthouse 성능 90+ 달성

- **T018: SEO 및 메타데이터 최적화** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: 인프라 | **예상 소요시간**: 3h
  - 페이지별 `generateMetadata`로 동적 title/description/OG 설정
  - `sitemap.ts`, `robots.ts` 추가
  - 구조화 데이터(JSON-LD `Article`) 삽입

- **T019: Vercel 배포 및 운영 설정** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: 인프라 | **예상 소요시간**: 3h
  - Vercel 프로젝트 연결 및 환경 변수 등록
  - 프리뷰/프로덕션 배포 워크플로우 검증
  - 배포 도메인에서 모든 핵심 플로우 Playwright MCP 재검증

- **T020: 모니터링 및 로깅 기반 마련** ⬜
  - **상태**: ⬜ 미시작 | **기능 ID**: 인프라 | **예상 소요시간**: 2h
  - Vercel Analytics 또는 간단한 콘솔 로거 도입
  - Notion API 호출 에러를 식별 가능한 로그 포맷으로 기록
  - 운영 중 발견되는 이슈를 백로그로 정리할 템플릿 마련

---

## 📊 전체 일정 요약

| Phase                | Task 개수 | 예상 누적 소요시간 | 상태      |
| -------------------- | --------- | ------------------ | --------- |
| Phase 0: 기반 정리   | 1         | 4h                 | ✅ 완료   |
| Phase 1: 골격 구축   | 3         | 10h                | ⬜ 미시작 |
| Phase 2: UI/UX 완성  | 6         | 24h                | ⬜ 미시작 |
| Phase 3: Notion 연동 | 6         | 19h                | ⬜ 미시작 |
| Phase 4: 운영 품질   | 4         | 12h                | ⬜ 미시작 |
| **합계**             | **20**    | **약 69h**         | —         |

> 💡 **개발 우선순위 원칙**: Phase 2까지는 더미 데이터로 UI/UX를 먼저 완성하고, Phase 3에서 실제 Notion API로 교체합니다. 이 구조 덕분에 백엔드(Notion 스키마)와 프론트엔드(UI)를 독립적으로 진척시킬 수 있고, MVP 시연이 빠르게 가능합니다.
